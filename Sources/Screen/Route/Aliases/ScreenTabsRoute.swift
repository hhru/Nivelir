#if canImport(UIKit)
import UIKit

/// Alias for the root route whose container type is `UITabBarController`.
///
/// - SeeAlso: `ScreenRoute`
/// - SeeAlso: `ScreenRootRoute`
public typealias ScreenTabsRoute = ScreenRootRoute<UITabBarController>
#endif
